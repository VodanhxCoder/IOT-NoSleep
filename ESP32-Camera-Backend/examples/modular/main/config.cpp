#include "config.h"

// Define global variables declared in config.h
volatile bool pauseStreamForCapture = false;
volatile bool captureRequested = false;
// isStreaming is defined in stream_manager.cpp
